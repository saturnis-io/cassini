
- Migrated 46 notes to Obsidian vault (spring cleaning)
- fix: Dashboard Cpk now uses backend `capability.cpk` instead of broken client-side calculation (was showing -1195.26 instead of 1.7555)
- fix: Explain API branching bug -- `limit`-only requests now correctly take chart-view path
- fix: Spec limit toggle on short-run Z charts -- `chart-domain.ts` and `ControlChart.tsx` Z-scale branches now include spec limits in domain bounds
- feat: Hide LSL/USL toolbar button for chart types where spec limits don't apply (CUSUM, EWMA, attribute, Pareto)
- Added lessons L-007 (no client-side capability) and L-008 (external domain overrides)